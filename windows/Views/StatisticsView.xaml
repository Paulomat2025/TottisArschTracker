<UserControl x:Class="ArtTimeTracker.Windows.Views.StatisticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ArtTimeTracker.Windows.ViewModels"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="4">
            <TextBlock Text="Statistiken" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16"/>

            <GroupBox Header="Zeit pro Tag (letzte 30 Tage)" Margin="0,0,0,16">
                <lvc:CartesianChart Height="260"
                                     Series="{Binding DailySeries}"
                                     XAxes="{Binding DailyXAxes}"
                                     YAxes="{Binding DailyYAxes}"/>
            </GroupBox>

            <GroupBox Header="Stunden pro Kunstwerk">
                <lvc:CartesianChart Height="260"
                                     Series="{Binding ArtworkSeries}"
                                     XAxes="{Binding ArtworkXAxes}"
                                     YAxes="{Binding ArtworkYAxes}"/>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
